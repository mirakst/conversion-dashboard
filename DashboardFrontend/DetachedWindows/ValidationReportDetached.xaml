<Window x:Class="DashboardFrontend.DetachedWindows.ValidationReportDetached"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:adorners="http://gu.se/Adorners"
        xmlns:converters="clr-namespace:DashboardFrontend.ValueConverters"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="Validation Report" Height="800" Width="1000"
        Background="{DynamicResource BackgroundColor}"
        FontFamily="Segoe UI"
        FontSize="15">
    <Window.Resources>
        <converters:ValidationsToStringConverter x:Key="ValidationsConverter"/>
        <converters:ShowSuccessfulManagersConverter x:Key="ShowSuccessfulManagersConverter"/>
    </Window.Resources>
    <Border BorderThickness="10"
            Background="{DynamicResource BackgroundColor}">
        <Grid>
            <TextBox Name="textBoxValidationReportSearchBar"
                     TextChanged="textBoxValidationReportSearchBar_TextChanged"
                     Margin="0,0,5,5"
                     Grid.Column="0"
                     Grid.Row="0"/>

            <ToggleButton Grid.Column="1"
                          Margin="4 0 4 5"
                          Style="{DynamicResource DefaultToggleButton}"
                          IsChecked="{Binding ShowSuccessfulManagers}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Show all"/>
                </StackPanel>
            </ToggleButton>
            
            <Button Content="Refresh"
                    Style="{DynamicResource DefaultButton}"
                    Margin="5,0,0,5"
                    Grid.Column="3"
                    Grid.Row="0"/>
            
            <DataGrid ItemsSource="{Binding Data}"
                      x:Name="validationsDataGrid"
                        AutoGenerateColumns="False"
                        Grid.Row="1"
                        Grid.ColumnSpan="3"
                        ScrollViewer.CanContentScroll="False"
                        GridLinesVisibility="None"
                        RowDetailsVisibilityMode="Collapsed" CanUserAddRows="False" CanUserDeleteRows="False" CanUserResizeRows="False" SelectionMode="Single" IsReadOnly="True" CanUserReorderColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Manager"
                                        Binding="{Binding ManagerNameFull}"
                                        Width="*">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="VerticalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Score"
                                        Width="120">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center" />
                                <Setter Property="VerticalAlignment" Value="Center" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Disabled"
                                        Binding="{Binding DisabledCount}"
                                        Width="64">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center" />
                                <Setter Property="VerticalAlignment" Value="Center" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Failed"
                                        Binding="{Binding FailedCount}"
                                        Width="64">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center" />
                                <Setter Property="VerticalAlignment" Value="Center" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Total"
                                        Binding="{Binding TotalCount}"
                                        Width="64">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center" />
                                <Setter Property="VerticalAlignment" Value="Center" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>

                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Style.Triggers>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding Converter={StaticResource ShowSuccessfulManagersConverter}}"
                                                Value="True"/>
                                    <Condition Binding="{Binding RelativeSource={RelativeSource AncestorType={x:Type DataGrid}},Path=DataContext.ShowSuccessfulManagers}"
                                                Value="False"/>
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </MultiDataTrigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="LightBlue"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="False">
                                <Setter Property="Background" Value="White"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>

                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="{x:Null}"/>
                                <Setter Property="Foreground" Value="Black"/>
                                <Setter Property="BorderBrush" Value="{x:Null}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.CellStyle>

                <DataGrid.RowHeaderTemplate>
                    <DataTemplate>
                        <ToggleButton Margin="5 0 0 0" Style="{DynamicResource ValidationExpandButton}" Click="ExpandRow_Click" />
                    </DataTemplate>
                </DataGrid.RowHeaderTemplate>
                <DataGrid.RowHeaderStyle>
                    <Style TargetType="{x:Type DataGridRowHeader}">
                        <Setter Property="VerticalAlignment" Value="Top"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Width" Value="30"/>
                    </Style>
                </DataGrid.RowHeaderStyle>

                <DataGrid.RowDetailsTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Vertical">
                            <Separator Margin="0,5,0,5"/>
                            
                            <ItemsControl ItemsSource="{Binding Tests}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Height="60">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="2*"/>
                                                <ColumnDefinition/>
                                                <ColumnDefinition/>
                                            </Grid.ColumnDefinitions>
                                            <Grid.RowDefinitions>
                                                <RowDefinition/>
                                                <RowDefinition/>
                                            </Grid.RowDefinitions>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{Binding Date,StringFormat=\(d/M HH:mm\)}"
                                                           Margin="0 0 5 0"/>
                                                <TextBlock Text="{Binding Status}"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal"
                                                        Grid.Row="1">
                                                <TextBlock Text="{Binding SrcCount,TargetNullValue='Src=N/A',StringFormat='Src=\{0\}'}"
                                                           Margin="0 0 50 0"/>
                                                <TextBlock Text="{Binding DstCount,TargetNullValue='Dst=N/A',StringFormat='Dst=\{0\}'}"/>
                                            </StackPanel>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                                <ItemsControl.Template>
                                    <ControlTemplate TargetType="ItemsControl">
                                        <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderBrush}">
                                            <ItemsPresenter/>
                                        </Border>
                                    </ControlTemplate>
                                </ItemsControl.Template>
                            </ItemsControl>
                            <Separator Margin="0,5,0,5"/>
                        </StackPanel>
                    </DataTemplate>
                </DataGrid.RowDetailsTemplate>
            </DataGrid>
                
            <StackPanel Orientation="Horizontal"
                        Grid.Row="2" Grid.ColumnSpan="3"
                        VerticalAlignment="Center">
                <TextBlock Text="{Binding OkCount,StringFormat='\{0\} OK'}" Foreground="{DynamicResource validationReportNormalColor}" Margin="0 0 50 0"/>
                <TextBlock Text="{Binding DisabledCount,StringFormat='\{0\} Disabled'}" Foreground="{DynamicResource validationReportDisabledColor}" Margin="0 0 50 0"/>
                <TextBlock Text="{Binding FailedCount,StringFormat='\{0\} Failed'}" Foreground="{DynamicResource validationReportErrorColor}" Margin="0 0 50 0"/>
            </StackPanel>
            
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="5*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="30"/>
            </Grid.RowDefinitions>
        </Grid>
    </Border>
</Window>
